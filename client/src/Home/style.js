import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  appBarSearch: {
    borderRadius: 4,
    marginButtom: "1rem",
    display: "flex",
    padding: "16px",
  },
  pagination: {
    borderRadius: 4,
    marginTop: "1rem",
    padding: "16px",
  },
}));

import { makeStyles } from '@mui/styles'

export default makeStyles(() => ({
  paper: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  spacing: {
    margin: '10px'
  }
  
}));